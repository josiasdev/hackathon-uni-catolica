import { ArrowLeft, Award, BookOpen, Clock3, Route, Sparkles, Wrench } from "lucide-react";
import Link from "next/link";
import { getAchievements, getExternalCertifications, getFullTrajectory, getProfile } from "@/lib/supabase/dal";
import { buttonVariants } from "@/components/ui/button";
import { Card, CardContent, CardDescription, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { XpProgress } from "@/components/xp-progress";
import { TrajectoryTimeline } from "@/components/trajectory-timeline";
import { AchievementGrid } from "@/components/achievement-grid";
import { ExternalCertificateForm } from "@/components/external-certificate-form";

export default async function StudentTrajectoryPage() {
  const profile = await getProfile();
  const [trajectory, achievements, external] = await Promise.all([getFullTrajectory(profile.id), getAchievements(profile.id), getExternalCertifications(profile.id)]);
  const completedCourses = trajectory.courses.filter((course) => course.status === "completed").length;
  return <div className="space-y-8 pb-10"><div><Link href="/student/dashboard" className="mb-6 inline-flex items-center gap-1 text-sm text-muted-foreground hover:text-foreground"><ArrowLeft className="size-4" /> Dashboard</Link><div className="flex items-center justify-between gap-4"><div className="flex items-center gap-3"><div className="flex size-12 items-center justify-center rounded-2xl bg-primary"><Route className="size-6" /></div><div><p className="text-xs font-semibold tracking-[0.2em] text-primary-foreground uppercase">Sua história em movimento</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Minha trajetória</h1></div></div><Link href={`/profile/${profile.id}`} className={buttonVariants({ variant: "outline" })}>Ver perfil público</Link></div><p className="mt-4 max-w-2xl text-muted-foreground">Tudo que você aprende, pratica e conquista fica organizado em uma linha do tempo viva.</p></div><XpProgress xp={trajectory.xp} /><section className="grid gap-4 sm:grid-cols-4"><Card><CardContent className="flex items-center gap-3 p-5"><BookOpen className="size-5 text-primary-foreground" /><div><p className="text-2xl font-semibold">{completedCourses}</p><p className="text-xs text-muted-foreground">Cursos concluídos</p></div></CardContent></Card><Card><CardContent className="flex items-center gap-3 p-5"><Wrench className="size-5 text-primary-foreground" /><div><p className="text-2xl font-semibold">{trajectory.skills.length}</p><p className="text-xs text-muted-foreground">Competências</p></div></CardContent></Card><Card><CardContent className="flex items-center gap-3 p-5"><Award className="size-5 text-primary-foreground" /><div><p className="text-2xl font-semibold">{trajectory.certifications.length + external.certificates.length}</p><p className="text-xs text-muted-foreground">Certificações</p></div></CardContent></Card><Card><CardContent className="flex items-center gap-3 p-5"><Clock3 className="size-5 text-primary-foreground" /><div><p className="text-2xl font-semibold">{external.experienceHours}h</p><p className="text-xs text-muted-foreground">Experiência registrada</p></div></CardContent></Card></section><Card><CardHeader className="border-b"><CardTitle className="flex items-center gap-2"><Sparkles className="size-5 text-primary-foreground" /> Linha do tempo</CardTitle><p className="text-sm text-muted-foreground">Um registro completo dos marcos que você construiu.</p></CardHeader><CardContent className="pt-6"><TrajectoryTimeline events={trajectory.events} skills={trajectory.skills} courses={trajectory.courses} certifications={trajectory.certifications} /></CardContent></Card><section><div className="mb-4"><p className="text-xs font-semibold tracking-widest text-primary-foreground uppercase">Reconhecimento</p><h2 className="mt-1 text-xl font-semibold">Conquistas</h2></div><AchievementGrid achievements={achievements} /></section><Card><CardHeader><CardTitle>Anexar certificado externo</CardTitle><CardDescription>Certificados que você já fez também contam para sua experiência.</CardDescription></CardHeader><CardContent><ExternalCertificateForm userId={profile.id} /></CardContent></Card><Card className="bg-muted"><CardContent className="flex items-start gap-4 p-5"><div className="flex size-10 shrink-0 items-center justify-center rounded-xl bg-background"><Route className="size-5" /></div><div><p className="font-medium">O próximo capítulo depende de você</p><p className="mt-1 text-sm leading-5 text-muted-foreground">Complete seu perfil, faça um curso ou adicione uma competência. Cada ação deixa sua trajetória mais forte.</p><Badge variant="outline" className="mt-3">Próximo marco: +25 XP ao completar o perfil</Badge></div></CardContent></Card></div>;
}